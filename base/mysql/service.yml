apiVersion: v1
kind: Service
metadata:
  name: db-svc
  labels:
    version: dev
  namespace: dev
spec:
  type: ClusterIP
  ports:
    - name: db-port
      port: 3306
      protocol: TCP
  selector:
    db: std-db